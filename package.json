{
  "name": "time-hostess",
  "version": "1.0.0-alpha.4.1",
  "private": true,
  "scripts": {
    "build": "next build",
    "export": "next export",
    "start": "next start -p 3000",
    "copyExport": "node --experimental-json-modules --experimental-vm-modules ./src/cli/copyExport.mjs",
    "out": "yarn build && yarn export && yarn copyExport",
    "dev": "NODE_OPTIONS='--inspect=8888' next dev"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "hot-cold-guide": "^1.0.0-alpha.6.0",
    "hot-cold-retext": "^1.0.0-alpha.2.0",
    "next": "^12.0.1",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "fs-extra": "^10.0.0"
  }
}
